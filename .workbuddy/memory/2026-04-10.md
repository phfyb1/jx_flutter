# 2026-04-10 工作记录

## SensorAlarmRule 正确逻辑（重要修正）

**SensorAlarmRule 替代 Python 脚本完整流程**：
1. 上传 Excel（.xlsx）→ 解析生成 alarm_rules.json（内存）
2. 从 JSON 生成 4 个 SQL 文件：
   - `1insert_single_rule.sql`（步骤2：报警规则）
   - `2insert_iot_alarm_algorithm.sql`（步骤3：报警算法）
   - `3insert_iot_alarm_rule_single_algorithm_rel.sql`（步骤4：规则-算法关联）
   - `4insert_iot_alarm_rule_single_sensor_rel.sql`（步骤5：传感器-规则关联）

**alarm_rules.json 字段结构**：
- RuleId, RuleName, RuleValue
- SensorsName, SensorsCode, SensorsID（从Excel解析，SensorsID 为空数组）
- low_low_alarm, low_alarm, low_equal, high_equal, high_alarm, high_high_alarm

**关键区分**：
- AlarmTool：参考 alarm-tool.html，**不需要**传感器JSON，算法ID全部在内存生成
- SensorAlarmRule：替代 Python 脚本（步骤0-6），**只读 Excel**，生成完整报警规则

## 修复记录

- SensorAlarmRuleController 重写：移除上传JSON逻辑，改为读Excel生成alarm_rules.json
- SensorAlarmRulePage 重写：上传Excel卡片+公司名称+4个SQL下载卡片+JSON预览弹窗
- AlarmToolController：修复 low_low_alarm 拼写、remark过滤 !='None'
